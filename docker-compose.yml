services:
    web:
        build:
            context: .
            dockerfile: Dockerfile
        image: finances-client:latest
        ports:
            - "80:80"
        restart: unless-stopped
        environment:
            - NODE_ENV=production
